# Security

Do not commit rendered configuration, audit output, IP inventories, AWS identifiers,
host keys, private keys, preshared keys, tokens, or credentials. Keep them under
`private/`, which is ignored.

Report security issues privately to the repository owner rather than opening a
public issue.

