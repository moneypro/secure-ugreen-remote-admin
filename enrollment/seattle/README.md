# Seattle public enrollment material

These are public keys only. Their private halves are stored on the Seattle home
server under the Git-ignored `private/keys/` directory.

Verify this directory against `SHA256SUMS` before installing a key on the NAS.
